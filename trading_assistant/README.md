# AI Candle Close Trading Assistant

Simple Flask web app for screenshot-based pre-trade and post-close confirmation signals.

## Run locally

```bash
cd trading_assistant
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Open http://127.0.0.1:8000.
